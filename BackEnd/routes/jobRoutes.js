const express = require('express');
const router = express.Router();
const pool = require('../db'); 

// Create Job Post
router.post('/job-posts', (req, res) => {
 
});

// Apply for a Job
router.post('/apply-for-job', (req, res) => {

});



module.exports = router;
